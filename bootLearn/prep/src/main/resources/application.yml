spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/prep?useSSL=true
    username: root
    password: MC@:(==ni2024
  main:
    banner-mode: off

server:
  port: 23046

logging:
  pattern:
    dateformat: HH:mm:ss

mybatis-plus:
  global-config:
    banner: false
  mapper-locations: classpath:com/demo/mapper/*.xml
  # 下面是打开sql语句输出
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

prep:
  # 图源文件路径，斜杠结尾
  dir:
    ../../../prepDir/
