<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.mapper.SubjectMapper">
    <!-- 分页查询-->
    <select id="getByPage" resultType="com.demo.model.entity.Subject">
        SELECT 
        subject_id, subject_name, exam_start_date, folder_name, theme_color, create_time
        FROM subject
    </select>
</mapper>
